upstream: 'http://localhost:5982'
sync_dir: '~/Sandbox'
username: 'test'
password: 'abc123'

log:
  -
    type: 'Screen::Color'
    stderr: 1
    min_level: info
    newline: true
    # option to allow a fallback logger in case on fails to load
